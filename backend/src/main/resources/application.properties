spring.application.name=backend

spring.datasource.url=jdbc:postgresql://aws-1-us-east-1.pooler.supabase.com:5432/postgres
spring.datasource.username=postgres.dckzghjwdibseemluukq
spring.datasource.password=yunaandnoa!
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
server.port=8080

spring.datasource.hikari.maximum-pool-size=1
spring.datasource.hikari.minimum-idle=1

# JWT auth (override APP_JWT_SECRET via env in any non-dev environment).
# Must be at least 32 bytes / 256 bits when base64-decoded for HS256.
app.jwt.secret=${APP_JWT_SECRET:ZGV2LW9ubHktc2VjcmV0LWRvLW5vdC11c2UtaW4tcHJvZHVjdGlvbi0xMjM0NTY3OA==}
app.jwt.expiration-ms=${APP_JWT_EXPIRATION_MS:86400000}
app.jwt.issuer=studylynk

# Allowed CORS origin for the React dev server.
app.cors.allowed-origin=${APP_CORS_ALLOWED_ORIGIN:http://localhost:3000}